// No templates exist yet: every current top-level view is route-bound (see
// components/pages/), not a reusable layout shared across different pages. Add here
// once something genuinely needs that distinction.
