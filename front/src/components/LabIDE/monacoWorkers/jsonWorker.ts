// See editorWorker.ts's own comment: not a real export, just runs the worker's own
// side-effect setup once evaluated inside the Worker global scope.
import '../../../../node_modules/monaco-editor/esm/vs/language/json/json.worker.js'
